SFML=-lsfml-graphics -lsfml-window -lsfml-system
DEBUG=-g

all: executables

debug: CXXFLAGS += $(DEBUG)
debug: executables

executables: $(PROJECT_PATH)$(PROJECT_NAME)

$(PROJECT_PATH)$(PROJECT_NAME): $(PROJECT_PATH)$(PROJECT_NAME).cpp	$(LIBPATH)shmupgine.h	\
			$(LIBPATH)systems.o 		\
			$(LIBPATH)entity.o 			\
			$(LIBPATH)graphicrenderer.o	\
			$(LIBPATH)bullet.o 			\
			$(LIBPATH)physics.o 		\
			$(LIBPATH)scene.o 			\
			$(LIBPATH)controls.o 		\
			$(LIBPATH)attribute.o 		\
			$(LIBPATH)destructor.o		\
			$(LIBPATH)gmanager.o		\
			$(LIBPATH)script.o
	$(CXX) $(CXXFLAGS)  -o $(PROJECT_PATH)$(PROJECT_NAME)	$(SFML) \
		$(PROJECT_PATH)$(PROJECT_NAME).cpp							\
		$(LIBPATH)systems.o			\
		$(LIBPATH)entity.o			\
		$(LIBPATH)graphicrenderer.o	\
		$(LIBPATH)bullet.o			\
		$(LIBPATH)physics.o			\
		$(LIBPATH)controls.o		\
		$(LIBPATH)attribute.o		\
		$(LIBPATH)destructor.o		\
		$(LIBPATH)scene.o			\
		$(LIBPATH)gmanager.o		\
		$(LIBPATH)script.o
